FIREBASE CONFIGURATION BACKUP
=====================================
Data: 2025-09-17
Motivo: Remoção temporária do Firebase para otimização de performance

ARQUIVOS MODIFICADOS:
--------------------
1. app/_layout.tsx - AuthProvider removido
2. src/contexts/AuthContext.tsx - Context desabilitado
3. src/services/firebaseConfig.ts - Configuração mantida mas não importada

CONFIGURAÇÕES FIREBASE:
----------------------
Project ID: formulatsetupmanager
API Key: AIzaSyBBocwz2Sp3E8KpXTaZNrUez4AMCuIDgXg
Auth Domain: formulatsetupmanager.firebaseapp.com
Storage Bucket: formulatsetupmanager.firebasestorage.app
Messaging Sender ID: 542753300240
App ID: 1:542753300240:android:c0c5d0e8ab526638d40f95

VARIÁVEIS DE AMBIENTE (.env):
----------------------------
EXPO_PUBLIC_API_KEY=AIzaSyBBocwz2Sp3E8KpXTaZNrUez4AMCuIDgXg
EXPO_PUBLIC_AUTH_DOMAIN=formulatsetupmanager.firebaseapp.com
EXPO_PUBLIC_PROJECT_ID=formulatsetupmanager
EXPO_PUBLIC_STORAGE_BUCKET=formulatsetupmanager.firebasestorage.app
EXPO_PUBLIC_MESSAGING_SENDER_ID=542753300240
EXPO_PUBLIC_APP_ID=1:542753300240:android:c0c5d0e8ab526638d40f95

ESTRUTURA ORIGINAL DO _LAYOUT.TSX:
----------------------------------
```tsx
import { Stack } from "expo-router";
import { GluestackUIProvider } from "../components/ui/gluestack-ui-provider";
import { AuthProvider } from "../src/contexts/AuthContext";
import { ThemeProvider, useTheme } from "../src/contexts/ThemeContext";
import '../global.css';

function AppContent() {
  const { theme } = useTheme();
  
  return (
    <GluestackUIProvider>
      <AuthProvider>
        <Stack screenOptions={{ headerShown: false }}>
          <Stack.Screen name="(tabs)" options={{ headerShown: false }} />
        </Stack>
      </AuthProvider>
    </GluestackUIProvider>
  );
}

export default function RootLayout() {
  return (
    <ThemeProvider>
      <AppContent />
    </ThemeProvider>
  );
}
```

DEPENDÊNCIAS FIREBASE NO PACKAGE.JSON:
-------------------------------------
"firebase": "^12.2.1"
"@react-native-google-signin/google-signin": "^16.0.0"

SCREENS QUE USAVAM FIREBASE:
---------------------------
- src/screens/AuthScreen.tsx (tela de login)
- src/screens/HomeScreen.tsx (signOut function)
- src/screens/CreateSetupScreen.tsx (user context)
- src/screens/SetupDetailsScreen.tsx (firestore operations)

PARA RESTAURAR A AUTENTICAÇÃO:
------------------------------
1. Restaurar AuthProvider no _layout.tsx
2. Reativar imports do Firebase nos screens
3. Configurar initializeAuth com AsyncStorage persistence:
   ```tsx
   import { initializeAuth, getReactNativePersistence } from 'firebase/auth';
   import ReactNativeAsyncStorage from '@react-native-async-storage/async-storage';
   const auth = initializeAuth(app, {
     persistence: getReactNativePersistence(ReactNativeAsyncStorage)
   });
   ```
4. Restaurar tela de login (AuthScreen.tsx)
5. Implementar navegação condicional baseada em auth state

NOTAS:
------
- AsyncStorage já está instalado: "@react-native-async-storage/async-storage": "^2.2.0"
- Google Sign-in configurado para development builds
- Firebase Firestore estava sendo usado para CRUD dos setups
- Auth state não persistia entre sessões (warning corrigido com AsyncStorage)
